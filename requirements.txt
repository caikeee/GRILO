# Core framework
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0,<1.0

# HTTP / utilities
httpx>=0.27.0,<1.0
requests>=2.32.0,<3.0
Pillow>=10.3.0,<12.0
python-dotenv>=1.0.1,<2.0

# Auth / crypto (security-sensitive — keep up to date)
python-jose[cryptography]>=3.5.0,<4.0
bcrypt>=4.2.0,<5.0

# AI / Groq
groq>=0.11.0,<1.0
tiktoken>=0.7.0,<1.0

# Database
sqlalchemy>=2.0.30,<3.0
psycopg2-binary>=2.9.9,<3.0
alembic>=1.13.0,<2.0

# Validation / settings (QW5 / QW6)
pydantic-settings>=2.4.0,<3.0
pydantic[email]>=2.8.0,<3.0

# Tooling
tenacity>=8.5.0,<10.0          # QW3 retries
slowapi>=0.1.9,<1.0            # QW10 rate limiting
python-json-logger>=2.0.7,<4.0 # QW12 logging
langdetect>=1.0.9,<2.0

# RAG / PDF
# NOTE: PyPDF2 is deprecated upstream; migrate `backend/rag/pdf_extractor.py` to `pypdf`
# at the next maintenance pass and replace the line below with `pypdf>=4.3.0,<6.0`.
pdfplumber>=0.11.0,<1.0
PyPDF2>=3.0.1,<4.0

# Tests
pytest>=8.0.0,<9.0
